# Install once per clone: uv run pre-commit install
# Run on demand: uv run pre-commit run --all-files
repos:
  - repo: local
    hooks:
      - id: ruff-format
        name: ruff format
        entry: .venv/Scripts/ruff format
        language: system
        types: [python]
      - id: ruff-check
        name: ruff check
        entry: .venv/Scripts/ruff check --fix
        language: system
        language: system
        types: [python]
